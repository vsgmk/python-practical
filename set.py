# 'add', 'clear', 'copy', 'difference', 'difference_update', 'discard', 'intersection', 'intersection_update', 'isdisjoint', 
# 'issubset', 'issuperset', 'pop', 'remove', 'symmetric_difference', 'symmetric_difference_update', 'union', 'update

a={1,2,3,4}
c={2,3}
b={5,2,4,5,1,2,221}
# print(a)
# print(dir(a))
# a.add(9) # it can add the number in set at last
# print(a)
# a.clear() # it can delete all elements present in the set
# print(a)
# b=a.copy() # it makes a copy of given set 
# print(b)
# c=a.difference(b) # it find all numbers presend in first in the second set if present then can print which is not present in second but present in first set
# print(c) # it uses third character for accesing the values
# a.difference_update(b) # it also work as difference but it cant take third letter for accesing the values
# print(a)
# a.discard(4) # it remove the element from the set which we are giving in discard(4) (4 is example only )
# print(a)
# c=a.intersection(b) # it find the same element present in the two set and print them by usign third variable for accesing the values 
# print(c)
# a.intersection_update(b) # it also work as intersection but it cant use third variable for accesing the values
# print(a)
# c=a.isdisjoint(b) # this dosjoint mean if not any element present in the both set then print Ture and if any one element is same in the then print False.
# print(c)
# print(c.issubset(a)) # if the given two sets and 1st set is subset of second set then it prints True
# print(a.issuperset(c)) # if first set is superset of second set then it prints True if not prints False
# a.pop() # it removes first element present in the set 
# print(a)
# a.remove(2) # it removes given number from the set.
# print(a)
# c=a.symmetric_difference(b) # it prints not same elements in second set 
# print(c)
# a.symmetric_difference_update(b)
# print(b)
# c=a.union(b) # it finds third set which have all elements present in both set 
# print(c)
# a.update(b) # to add all elements present in second set into the first sert an print it 
# print(a)
